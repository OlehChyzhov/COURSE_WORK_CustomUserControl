<Window x:Class="CustomUserControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomUserControl"
        xmlns:c="clr-namespace:CustomControlsLibrary;assembly=CustomControlsLibrary"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Width="200">
            <Button Content="RadioButtonGroup" FontSize="20" Click="RadioButtonGroup"/>
            <Button Content="CheckBoxGroup" FontSize="20" Click="CheckBoxGroup"/>
            <Button Content="StagedProgressBar" FontSize="20" Click="StagedProgressBar"/>
            <Button Content="StagedProgressBarTemplates" FontSize="16" Click="StagedProgressBarTemplates"/>
        </StackPanel>

        <Frame Grid.Column="1" x:Name="MainFrame"/>
    </Grid>
</Window>
