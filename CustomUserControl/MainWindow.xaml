<Window x:Class="CustomUserControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomUserControl"
        xmlns:c="clr-namespace:CustomControlsLibrary;assembly=CustomControlsLibrary"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer>
            <StackPanel Width="200">
                <Button Content="Staged PB" FontSize="16" Padding="5" Click="StagedPB"/>
                <Button Content="Staged PB Template" FontSize="16" Padding="5" Click="StagedPBTemplate"/>
                <Button Content="Dependency Property" FontSize="16" Padding="5" Click="DependencyPropPage"/>
                <Button Content="Attached Property" FontSize="16" Padding="5" Click="AttachedPropPage"/>
                <Button Content="C4. Design" FontSize="16" Padding="5" Click="Chapter4Design"/>
                <Button Content="C5. Functionality" FontSize="16" Padding="5" Click="Chapter5Functionality"/>
                
                <Button Content="RadioButtonGroup Final" FontSize="16" Padding="5" Click="RadioButtonGroup"/>
                <Button Content="CheckBoxGroup Final" FontSize="16" Padding="5" Click="CheckBoxGroup"/>
            </StackPanel>
        </ScrollViewer>
        <Frame Grid.Column="1" x:Name="MainFrame"/>
    </Grid>
</Window>
